{
  "scripts": {
    "copy:ui": "cp src/ui.html dist/ui.html",
    "build": "esbuild src/code.ts --bundle --outfile=dist/code.js --format=iife --platform=browser",
    "watch": "esbuild src/code.ts --bundle --outfile=dist/code.js --format=iife --platform=browser --watch",
    "dev": "npm run watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.95.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.0.0"
  }
}
